{
  "max_size_mb": {
    "type": "int",
    "description": "允许下载的最大谱面文件大小（MB）",
    "hint": "设置为 0 表示不限制文件大小。",
    "default": 20
  },
  "cleanup_delay_seconds": {
    "type": "int",
    "description": "文件发送后延迟清理的秒数",
    "hint": "至少为 10 秒，避免消息平台尚未读取文件时提前删除。",
    "default": 120
  }
}
